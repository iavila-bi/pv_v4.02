<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nuevo Ticket | Sistema PostVenta</title>

    <link rel="stylesheet" href="estilos.css">

    <style>
        /* --- ESTILOS VISUALES (MISMOS QUE EL MENÚ) --- */
        :root {
            --bg-body: #f0f4f8;
            --bg-card: rgba(255, 255, 255, 0.95);
            --text-main: #2c3e50;
            --text-secondary: #7f8c8d;
            --accent-blue: #2980b9;
            --accent-glow: rgba(41, 128, 185, 0.3);
            --border: #e1e4e8;
            --input-bg: #ffffff;
            --section-bg: #f8f9fa; /* Color de fondo suave para cada bloque */
        }

        body.dark-mode {
            --bg-body: #0f1115;
            --bg-card: rgba(30, 34, 42, 0.95);
            --text-main: #ecf0f1;
            --text-secondary: #bdc3c7;
            --accent-blue: #3498db;
            --accent-glow: rgba(52, 152, 219, 0.15);
            --border: #2c3e50;
            --input-bg: #2c3e50;
            --section-bg: #1e2329;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg-body);
            color: var(--text-main);
            margin: 0;
            padding: 40px 20px;
            min-height: 100vh;
        }

        /* --- FONDO ANIMADO --- */
        .background-shapes { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; overflow: hidden; }
        .shape { position: absolute; border-radius: 50%; background: var(--accent-glow); filter: blur(80px); animation: float 10s infinite alternate; }
        .shape-1 { width: 500px; height: 500px; top: -100px; left: -100px; }
        .shape-2 { width: 600px; height: 600px; bottom: -150px; right: -100px; animation-delay: 5s; }
        @keyframes float { 0% { transform: translate(0,0); } 100% { transform: translate(20px, 40px); } }

    /* --- CONTENEDOR PRINCIPAL (ESTILO DASHBOARD / PANTALLA COMPLETA) --- */
        .form-container {
            width: 96%;             /* Ocupa el 96% del ancho de la pantalla */
            max-width: 1800px;       /* Un límite gigante por si usas una TV 4K */
            margin: 0 auto;          /* Centrado */
            background-color: var(--bg-card);
            backdrop-filter: blur(15px);
            padding: 30px;           /* Un poco menos de padding para ganar espacio */
            border-radius: 15px;
            border: 1px solid var(--border);
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            min-height: 85vh;        /* Para que ocupe buena parte de la altura también */
            display: flex;
            flex-direction: column;
        }

        /* HEADER */
        header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; }
        h1 { margin: 0; font-size: 2rem; color: var(--accent-blue); }
        .btn-back { text-decoration: none; color: var(--text-secondary); font-weight: 600; }
        .theme-toggle { cursor: pointer; background: none; border: none; font-size: 1.5rem; }

        /* --- SECCIONES (LOS 4 FORMULARIOS) --- */
        .form-section {
            background-color: var(--section-bg);
            border: 1px solid var(--border);
            padding: 25px;
            border-radius: 12px;
            margin-bottom: 30px;
            position: relative;
        }
        
        .form-section h3 {
            margin-top: 0;
            margin-bottom: 20px;
            color: var(--accent-blue);
            font-size: 1.1rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-bottom: 2px solid var(--border);
            padding-bottom: 10px;
        }

        .section-number {
            position: absolute;
            top: -15px;
            right: 20px;
            background: var(--accent-blue);
            color: white;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }

        /* --- GRID Y INPUTS --- */
        .row { display: flex; gap: 20px; margin-bottom: 15px; }
        .col { flex: 1; display: flex; flex-direction: column; }
        @media (max-width: 700px) { .row { flex-direction: column; gap: 10px; } }

        label { font-weight: 600; margin-bottom: 6px; font-size: 0.85rem; color: var(--text-secondary); }
        
        input, select, textarea {
            padding: 10px;
            border: 1px solid var(--border);
            border-radius: 6px;
            font-size: 0.95rem;
            background-color: var(--input-bg);
            color: var(--text-main);
        }
        
        /* Inputs Readonly (Automáticos) */
        input[readonly] { background-color: rgba(0,0,0,0.05); border-color: transparent; font-weight: bold; }

        /* --- BOTÓN GUARDAR --- */
        .btn-submit {
            width: 100%;
            background: linear-gradient(135deg, #2980b9 0%, #2c3e50 100%);
            color: white;
            padding: 18px;
            border: none;
            border-radius: 10px;
            font-size: 1.2rem;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s;
        }
        .btn-submit:hover { transform: scale(1.02); }

    </style>
</head>
<body>

    <div class="background-shapes">
        <div class="shape shape-1"></div>
        <div class="shape shape-2"></div>
    </div>

    <div class="form-container">
        <header>
            <a href="menu.html" class="btn-back">← Volver</a>
            <h1>Registro de PostVenta</h1>
            <button class="theme-toggle" id="themeBtn">🌙</button>
        </header>

        <form id="formPostventa">
            
            <div class="form-section">
                <div class="section-number">1</div>
                <h3>Identificación de la Propiedad</h3>
                
                <div class="row">
                    <div class="col">
                        <label>1. Proyecto</label>
                        <select id="proyectoSelect" required><option value="">Cargando...</option></select>
                    </div>
                    <div class="col">
                        <label>Selección de Vivienda (Casa/Depto)</label>
                        <select id="casaSelect" disabled required><option value="">← Seleccione proyecto</option></select>
                    </div>
                </div>

                <div class="row">
                    <div class="col"><label>2. Modelo</label><input type="text" id="infoModelo" readonly placeholder="---"></div>
                    <div class="col"><label>3. Tren / Manzana</label><input type="text" id="infoTren" readonly placeholder="---"></div>
                    <div class="col"><label>4. Orientación</label><input type="text" id="infoOrientacion" readonly placeholder="---"></div>
                </div>
                <div class="row">
                    <div class="col"><label>5. Fecha Entrega</label><input type="text" id="infoFecha" readonly placeholder="---"></div>
                    <div class="col"><label>6. Status Garantía</label><input type="text" id="infoGarantia" readonly placeholder="---"></div>
                </div>
            </div>


           <div class="form-section">
                <div class="section-number">2</div>
                <h3>Estado y Cliente</h3>
                
                <div class="row">
                    <div class="col">
                        <label>1. Estado del Registro (Status)</label>
                        <select id="estadoRegistro"> <option value="Pendiente">Pendiente</option>
                            <option value="Cerrada">Cerrada</option>
                            <option value="En preparación">En preparación</option>
                            <option value="En proceso">En proceso</option>
                            <option value="En stock">En stock</option>
                            <option value="Entregada">Entregada</option>
                            <option value="N/A">N/A</option>
                            <option value="Pendientes">Pendientes</option>
                        </select>
                    </div>
                    <div class="col">
                        <label>4. Origen</label>
                        <select id="origenSolicitud"> <option value="Correo">Correo</option>
                            <option value="Correo">Correo</option>
                            <option value="Entidad patrocinante">Entidad patrocinante</option>
                            <option value="Jocelin">Jocelin</option>
                            <option value="MDA">MDA</option>
                            <option value="Nuevas / Entrega">Nuevas / Entrega</option>
                            <option value="Serviu">Serviu</option>
                            <option value="Terreno">Terreno</option>  
                        </select>
                    </div>
                </div>
                <div class="row">
                    <div class="col">
                        <label>2. Cliente / Propietario</label>
                        <input type="text" id="nombreCliente" placeholder="Nombre completo" required>
                    </div>
                    <div class="col">
                        <label>3. Contacto (Tel/Email)</label>
                        <input type="text" id="contactoCliente" placeholder="+569..." required>
                    </div>
                </div>    
            </div>

            <div class="form-section">
                <div class="section-number">3</div>
                <h3>Registro por Familia</h3>
                
                <div class="row">
                    <div class="col"><label>1. Familia</label><select id="familiaSelect" required><option value="">Cargando...</option></select></div>
                    <div class="col"><label>2. Sub-Familia</label><select id="subfamiliaSelect" disabled><option value="">---</option></select></div>
                    <div class="col"><label>3. Recinto</label><select id="recintoSelect" required><option value="">Cargando...</option></select></div>
                </div>

                <div class="row">
                    <div class="col">
                        <label>4. Comentarios Clientes Previos</label>
                        <textarea id="comentarios" rows="3" placeholder="Descripción del problema..."></textarea>
                    </div>
                </div>

                <div class="row">
                    <div class="col"><label>5. Fecha Levantamiento</label><input type="date" id="fechaLevantamiento"></div>
                    <div class="col"><label>6. Fecha Visita</label><input type="date" id="fechaVisita" required></div>
                </div>

                <div class="row">
                    <div class="col"><label>7. Responsable</label><select id="responsableSelect"><option value="">Cargando...</option></select></div> </div>

                <label>8. Acciones realizadas / Observaciones</label>
                <textarea id="accionesObservaciones" rows="2" placeholder="Detalle técnico de la solución..."></textarea> </div>


            <div class="form-section">
                <div class="section-number">4</div>
                <h3>Ejecución y Costos</h3>

                <div class="row">
                    <div class="col">
                        <label>1. Ejecutante (Maestro/Contratista)</label>
                        <select id="ejecutanteSelect"><option value="">(Sin asignar)</option></select>
                    </div>
                    <div class="col">
                        <label>4. Tarea Específica</label>
                        <input type="text" id="descTarea" placeholder="Ej: Cambio de flexibles">
                    </div>
                </div>

                <div class="row">
                    <div class="col"><label>2. Fecha Inicio</label><input type="date" id="fechaInicio"></div>
                    <div class="col"><label>3. Fecha Término</label><input type="date" id="fechaTermino"></div>
                    <div class="col"><label>5. Horas Día</label><input type="number" id="horasDia" value="0" min="0" step="0.5"></div>
                </div>
            </div>

            <button type="submit" class="btn-submit">💾 GUARDAR TICKET COMPLETO</button>
        </form>
    </div>

    <script src="logica.js"></script>
    <script>
        // Lógica de Modo Oscuro
        const btn = document.getElementById('themeBtn');
        const body = document.body;
        if(localStorage.getItem('theme') === 'dark') { body.classList.add('dark-mode'); btn.textContent = '☀️'; }
        btn.addEventListener('click', () => {
            body.classList.toggle('dark-mode');
            const isDark = body.classList.contains('dark-mode');
            btn.textContent = isDark ? '☀️' : '🌙';
            localStorage.setItem('theme', isDark ? 'dark' : 'light');
        });

        // Fecha de hoy automática
        const hoy = new Date();
        document.getElementById('fechaLevantamiento').valueAsDate = hoy;
    </script>
</body>
</html